{
    "name": "Python 3",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "..",
    },
    "extensions": [
        "ms-python.python",
        "charliermarsh.ruff",
	],
    "postCreateCommand": "pip install -r ${containerWorkspaceFolder}/install/requirements-dev.txt"
}
